<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".Menu"
    android:background="@color/background" >

    <Button
        android:id="@+id/active_alarms"
        android:layout_width="115dp"
        android:layout_height="65dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="91dp"
        android:text="@string/active_alarms"
        android:background="@layout/buttons" />

    <Button
        android:id="@+id/historical_alarms"
        android:layout_width="115dp"
        android:layout_height="65dp"
        android:layout_alignLeft="@+id/active_alarms"
        android:layout_below="@+id/active_alarms"
        android:layout_marginTop="77dp"
        android:text="@string/historical_alarms"
        android:background="@layout/buttons" />

</RelativeLayout>